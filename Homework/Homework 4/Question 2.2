list = [1, 2, 3]
def square(list): 
    values = [] 
    for i in list: 
        values.append(i** 2)
    return values

print(square(list))

# error, I could not get my code to run because I realized I did not include the final print line. No diagnostic helped, I had to look it up lol
